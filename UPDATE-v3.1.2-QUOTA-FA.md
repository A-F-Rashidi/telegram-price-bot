# Patch v3.1.2 — مدیریت سهمیه Nerkh و Backoff

این Patch روی v3.1.1 اعمال می‌شود و اتصال Nerkh API v1 را نگه می‌دارد، اما مصرف سهمیه را کنترل می‌کند.

## تغییرات اصلی

- Nerkh دیگر در Cron هر دقیقه فراخوانی نمی‌شود.
- دریافت کامل Nerkh به‌صورت پیش‌فرض هر **۶ ساعت** انجام می‌شود.
- هر دریافت کامل Nerkh شامل سه endpoint رسمی است:
  - Currency
  - Gold
  - Crypto
- با فاصله ۶ ساعته، مصرف پایه حدود ۱۲ درخواست در روز / ۳۶۰ درخواست در ۳۰ روز است.
- بین نوبت‌های Nerkh، ربات از TGJU و سپس Navasan/GitHub استفاده می‌کند.
- آخرین نتیجه موفق Nerkh در D1 کش می‌شود و فقط برای پرکردن نمادهای جاافتاده استفاده می‌شود؛ قیمت تازه TGJU/Navasan را overwrite نمی‌کند.

## مدیریت HTTP 460 / QUOTA_EXCEEDED

- HTTP 460 دیگر «توکن نامعتبر» محسوب نمی‌شود.
- وضعیت آن به‌صورت `quota` ذخیره می‌شود.
- پس از 460، ربات تا ۶ ساعت Nerkh را دوباره صدا نمی‌زند.
- Cronهای یک‌دقیقه‌ای در این بازه مستقیماً به fallback می‌روند.
- دریافت سه endpoint به‌صورت ترتیبی انجام می‌شود؛ اگر endpoint اول 460/429/auth بدهد، دو درخواست بعدی ارسال نمی‌شوند.

## پنل مدیریت

دکمه «وضعیت منابع» دیگر تست زنده API نمی‌زند و بنابراین سهمیه Nerkh مصرف نمی‌کند. پنل از وضعیت ذخیره‌شده در D1 نمایش می‌دهد:

- آخرین اتصال واقعی
- وضعیت quota/rate-limit/auth
- آخرین خطا
- تعداد نمادهای کش‌شده
- زمان تقریبی نوبت بعدی Nerkh

## ثبت توکن جدید

هنگام ثبت توکن جدید فقط یک بار اعتبارسنجی و تست انجام می‌شود. اگر تست موفق باشد، همان نتیجه در D1 کش می‌شود و Cron بلافاصله دوباره Nerkh را فراخوانی نمی‌کند.

اگر Nerkh هنگام ثبت توکن 460 بدهد، توکن ذخیره می‌شود و پیام واضح «سهمیه پر شده» نمایش داده می‌شود؛ توکن حذف یا نامعتبر علامت‌گذاری نمی‌شود.

## نصب

فایل‌های Patch را روی پوشه پروژه Replace کنید و سپس:

```powershell
npm test
npx wrangler deploy
```

نیازی به تغییر schema دیتابیس D1 نیست؛ کلیدهای جدید داخل جدول عمومی `bot_state` ذخیره می‌شوند.

## تغییر فاصله Nerkh

اگر در آینده پلن Nerkh با سهمیه بالاتر گرفتید، مقدار زیر در `src/config.js` قابل تغییر است:

```js
nerkhRefreshIntervalMs: 6 * 60 * 60 * 1000
```

مثلاً هر یک ساعت:

```js
nerkhRefreshIntervalMs: 60 * 60 * 1000
```

برای پلن فعلی با سقف ماهانه ۴۶۵ درخواست، مقدار ۶ ساعت امن‌تر است.
