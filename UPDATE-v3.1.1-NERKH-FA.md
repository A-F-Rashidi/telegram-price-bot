# Patch v3.1.1 — اتصال Nerkh.io API v1

این پچ برای پروژه v3.0.1/v3.1.0 ساخته شده و تغییرات قالب و نقره نسخه 3.1.0 را نیز حفظ می‌کند.

## چه چیزی اصلاح شد؟

اتصال قدیمی Nerkh از آدرس `https://api.nerkh.io/v2/prices/json/lite` حذف شد و اتصال مطابق مستندات فعلی Nerkh به API v1 انجام می‌شود:

- اعتبارسنجی توکن: `POST https://api.nerkh.io/v1/auth/validate`
- ارزها: `GET https://api.nerkh.io/v1/prices/json/currency`
- طلا و سکه: `GET https://api.nerkh.io/v1/prices/json/gold`
- کریپتو: `GET https://api.nerkh.io/v1/prices/json/crypto`
- احراز هویت: `Authorization: Bearer <TOKEN>`

سه پاسخ قیمت با هم ادغام می‌شوند و نمادهای موردنیاز ربات از آن‌ها انتخاب می‌شوند.

## Fallback هوشمند

اگر Nerkh یک نماد را نداشته باشد (مثلاً نقره 999 در بعضی پلن/پاسخ‌ها)، TGJU آن نماد را تکمیل می‌کند.

اگر Nerkh قیمت را داشته باشد اما درصد تغییر را ندهد، قیمت Nerkh حفظ می‌شود و فقط درصد تغییر از TGJU تکمیل می‌شود.

## پنل مدیریت

دکمه «وضعیت منابع» حالا یک تست زنده انجام می‌دهد و به‌جای اینکه صرفاً بگوید توکن ذخیره شده، این موارد را نشان می‌دهد:

- موجود بودن توکن
- نتیجه اتصال واقعی Nerkh
- نتیجه اعتبارسنجی توکن
- وضعیت endpointهای ارز / طلا و سکه / کریپتو
- تعداد نمادهای دریافت‌شده
- خطای واقعی HTTP مثل 401، 403، 429، timeout و ...

## نصب

فایل‌های ZIP را روی پوشه پروژه کپی و Replace کنید، سپس:

```powershell
npm test
npx wrangler deploy
```

بعد در ربات:

`پنل مدیریت → وضعیت منابع`

را بزنید.

اگر توکن قبلاً در خود ربات ذخیره شده باشد، معمولاً نیازی به ورود دوباره نیست. با این حال اگر اتصال ناموفق بود، یک بار از گزینه «ثبت / تغییر API Nerkh» همان توکن را دوباره ثبت کنید.

## نتیجه تست

32 تست Node.js پاس شده‌اند.

> نکته: dry-run Wrangler در محیط سازنده پچ اجرا نشد، چون پروژه Windows Ready شامل باینری Windows برای workerd است و محیط ساخت پچ Linux است. تست‌های Node.js و syntax check کامل پاس شده‌اند.
